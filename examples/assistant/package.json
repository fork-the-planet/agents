{
  "name": "@cloudflare/agents-assistant-example",
  "description": "General-purpose assistant with workspace tools — read, write, edit, find, grep files",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "start": "vite dev",
    "deploy": "vite build && wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false"
  },
  "dependencies": {
    "@cloudflare/agents-ui": "*",
    "@cloudflare/kumo": "^1.10.0",
    "@cloudflare/think": "*",
    "@phosphor-icons/react": "^2.1.10",
    "@streamdown/code": "^1.0.2",
    "agents": "*",
    "streamdown": "^2.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4.2.1"
  }
}
